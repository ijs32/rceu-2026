# General structure
src contains code for Ian and Mahmoudah, as well as a shared directory

shared contains functions that will likely be used across many files. You can import them with:

```python
from shared.file import function
```